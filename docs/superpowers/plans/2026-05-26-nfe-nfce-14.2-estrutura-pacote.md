# NFe/NFCe — Slice 14.2: Estrutura do pacote Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Criar o pacote `TecnoFisc.Sped.NFeNFCe` (read-only, leiaute 4.00) com a casca da API pública — `IDocumentoFiscalXml` (contrato comum), `ParserNFeOptions` e o esqueleto de `ParserNFe` — e alinhar `ARCHITECTURE.md` à fusão Stage 14+15.

**Architecture:** Novo projeto `src/TecnoFisc.Sped.NFeNFCe/` (referencia só o Core, **sem** source generator — não há `RegistroSped` na v1) + projeto de testes, ambos registrados no `.slnx`. O contrato transversal `IDocumentoFiscalXml` reside no **Core** (`Core/Xml/`), ao lado de `IdentificadorXmlFiscal`/`TipoDocumentoFiscalXml`, porque é transversal a leiautes XML fiscais (NF-e/NFC-e hoje, CT-e no futuro) — regra `ARCHITECTURE.md` §4.2 "transversal → Core". `ParserNFe`/`ParserNFeOptions` ficam no namespace raiz `TecnoFisc.Sped.NFeNFCe` (API plana). O corpo de desserialização **não** entra nesta slice (vem na 14.3): `ReadAsync` valida argumentos e lança `NotImplementedException`.

**Tech Stack:** .NET 10, xUnit v3 + FluentAssertions, `System.Xml.XmlReader`. Fonte de verdade da slice: `sped/STAGE_14_NFE_NFCE.md` (§0 deltas, §3 `ParserNFeOptions`, §4 modelagem, §5 API, §8 tabela de slices).

---

## ⚠️ Build pitfalls (leia antes de começar)

O repositório usa `TreatWarningsAsErrors=true` + `EnforceCodeStyleInBuild=true` + `ImplicitUsings=enable` (ver `Directory.Build.props`). Consequências que **já quebraram build** antes:

1. **IDE0005 (using redundante):** projetos `src/` **não têm** `GlobalUsings.cs` — usam `using` por arquivo (só `System.*` vem do `ImplicitUsings`). Todo arquivo que referencia tipos do projeto (`ChaveAcesso`, etc.) precisa do `using` explícito. Já os projetos de **teste** têm `GlobalUsings.cs`: **não** repita num arquivo de teste um `using` que já está no `GlobalUsings.cs` (vira IDE0005 → erro). Core.Tests `GlobalUsings.cs` tem apenas `FluentAssertions`, `TecnoFisc.Sped.Core.ValueObjects`, `Xunit` — portanto `TecnoFisc.Sped.Core.Xml` **precisa** de `using` por arquivo em Core.Tests.
2. **CA1822 (mark as static):** `ReadAsync` no esqueleto não lê estado de instância → CA1822 pode sugerir `static`. **CA1822 é severidade _info_, não _warning_** → **não** quebra o build sob `TreatWarningsAsErrors`. Confirme rodando o build (Step de cada task). Caso, e somente caso, o build acuse CA1822 como erro neste repo, referencie `Options` no corpo do método (`_ = Options;`) — mas só se realmente falhar.
3. Após cada implementação rode `dotnet build TecnoFisc.Sped.slnx` e confirme **0 warnings, 0 errors** antes de commitar.

---

## Task 0: Branch de trabalho

**Files:** nenhum (operação git).

- [ ] **Step 1: Criar a branch a partir de `dev` atualizado**

```bash
git switch dev
git pull --ff-only
git switch -c feat/nfe-14.2-estrutura-pacote
```

- [ ] **Step 2: Confirmar branch**

Run: `git branch --show-current`
Expected: `feat/nfe-14.2-estrutura-pacote`

---

## Task 1: Scaffold dos projetos (src + testes) e registro no `.slnx`

Cria o projeto de produção, o de testes e os registra na solução. Ao final a solução compila com os dois projetos vazios.

**Files:**
- Create: `src/TecnoFisc.Sped.NFeNFCe/TecnoFisc.Sped.NFeNFCe.csproj`
- Create: `tests/TecnoFisc.Sped.NFeNFCe.Tests/TecnoFisc.Sped.NFeNFCe.Tests.csproj`
- Create: `tests/TecnoFisc.Sped.NFeNFCe.Tests/GlobalUsings.cs`
- Modify: `TecnoFisc.Sped.slnx`

- [ ] **Step 1: Criar o `.csproj` de produção**

`src/TecnoFisc.Sped.NFeNFCe/TecnoFisc.Sped.NFeNFCe.csproj`:

```xml
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Description>Implementação .NET dos documentos fiscais eletrônicos em XML — NF-e (modelo 55) e NFC-e (modelo 65), leiaute 4.00. Pacote read-only: parser order-independent (tolera o XML canônico e o envelope SERPRO) + modelo tipado, sem geração/emissão.</Description>
    <PackageId>TecnoFisc.Sped.NFeNFCe</PackageId>
    <PackageTags>sped;nfe;nfce;nf-e;nfc-e;nota-fiscal;xml;fiscal;brasil;tecnofisc</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\TecnoFisc.Sped.Core\TecnoFisc.Sped.Core.csproj" />
  </ItemGroup>

</Project>
```

> Nota: **sem** referência ao source generator (diferente de Ecd/EfdContribuicoes). O generator só emite catálogo para tipos `RegistroSped`, que não existem aqui — §3 do STAGE_14 trava "sem source generator na v1".

- [ ] **Step 2: Criar o `.csproj` de testes**

`tests/TecnoFisc.Sped.NFeNFCe.Tests/TecnoFisc.Sped.NFeNFCe.Tests.csproj`:

```xml
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit.v3" />
    <PackageReference Include="xunit.runner.visualstudio">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="FluentAssertions" />
    <PackageReference Include="coverlet.collector">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\TecnoFisc.Sped.NFeNFCe\TecnoFisc.Sped.NFeNFCe.csproj" />
  </ItemGroup>

</Project>
```

- [ ] **Step 3: Criar `GlobalUsings.cs` do projeto de testes**

`tests/TecnoFisc.Sped.NFeNFCe.Tests/GlobalUsings.cs` — **só** os namespaces usados na 14.2 (IDE0005 reclama de global using não usado):

```csharp
global using FluentAssertions;
global using Xunit;
```

> **Não** incluir `global using TecnoFisc.Sped.NFeNFCe;`. As classes de teste ficam no namespace `TecnoFisc.Sped.NFeNFCe.Tests`, do qual `TecnoFisc.Sped.NFeNFCe` é namespace **ancestral** — `ParserNFe`/`ParserNFeOptions` (raiz do pacote) são alcançáveis sem `using`, e o global using viraria redundante (IDE0005). Convenção do repo (cf. `TecnoFisc.Sped.Ecd.Tests`). Slices futuras acrescentam `Core.ValueObjects`/`Core.Xml` ao global using quando precisarem.

- [ ] **Step 4: Registrar os dois projetos no `.slnx`**

Em `TecnoFisc.Sped.slnx`, adicionar a linha de `src` após `Ecd` (dentro de `<Folder Name="/src/">`):

```xml
    <Project Path="src/TecnoFisc.Sped.Ecd/TecnoFisc.Sped.Ecd.csproj" />
    <Project Path="src/TecnoFisc.Sped.NFeNFCe/TecnoFisc.Sped.NFeNFCe.csproj" />
```

E a linha de teste após `Ecd.Tests` (dentro de `<Folder Name="/tests/">`):

```xml
    <Project Path="tests/TecnoFisc.Sped.Ecd.Tests/TecnoFisc.Sped.Ecd.Tests.csproj" />
    <Project Path="tests/TecnoFisc.Sped.NFeNFCe.Tests/TecnoFisc.Sped.NFeNFCe.Tests.csproj" />
```

- [ ] **Step 5: Compilar a solução**

Run: `dotnet build TecnoFisc.Sped.slnx`
Expected: build OK, 0 errors, 0 warnings. O projeto de testes compila mesmo sem nenhum teste ainda.

- [ ] **Step 6: Commit**

```bash
git add src/TecnoFisc.Sped.NFeNFCe tests/TecnoFisc.Sped.NFeNFCe.Tests TecnoFisc.Sped.slnx
git commit -m "feat(nfe): scaffold do pacote TecnoFisc.Sped.NFeNFCe (slice 14.2)"
```

---

## Task 2: `IDocumentoFiscalXml` (contrato comum, no Core)

Interface marcadora dos documentos fiscais XML, exposta pelo `ParserNFe`. Mora no Core por ser transversal (§4.2). Expõe a `ChaveAcesso` — a chave de correlação nota × eventos (§6.1).

**Files:**
- Create: `src/TecnoFisc.Sped.Core/Xml/IDocumentoFiscalXml.cs`
- Test: `tests/TecnoFisc.Sped.Core.Tests/Xml/IDocumentoFiscalXmlTests.cs`

- [ ] **Step 1: Escrever o teste que falha**

`tests/TecnoFisc.Sped.Core.Tests/Xml/IDocumentoFiscalXmlTests.cs`:

```csharp
using TecnoFisc.Sped.Core.Xml;

namespace TecnoFisc.Sped.Core.Tests.Xml;

public class IDocumentoFiscalXmlTests
{
    private const string ChaveValida = "35240111222333000181550010000000011000000018";

    private sealed record DocumentoFake(ChaveAcesso ChaveAcesso) : IDocumentoFiscalXml;

    [Fact]
    public void Documento_Exposes_ChaveAcesso_Through_Contract()
    {
        IDocumentoFiscalXml documento = new DocumentoFake(ChaveAcesso.Create(ChaveValida));

        documento.ChaveAcesso.ToString().Should().Be(ChaveValida);
    }
}
```

- [ ] **Step 2: Rodar o teste e confirmar a falha de compilação**

Run: `dotnet test TecnoFisc.Sped.slnx --filter "FullyQualifiedName~IDocumentoFiscalXml"`
Expected: FALHA de compilação — `IDocumentoFiscalXml` não existe.

- [ ] **Step 3: Implementar a interface**

`src/TecnoFisc.Sped.Core/Xml/IDocumentoFiscalXml.cs`:

```csharp
using TecnoFisc.Sped.Core.ValueObjects;

namespace TecnoFisc.Sped.Core.Xml;

/// <summary>
/// Contrato comum dos documentos fiscais eletrônicos em XML (NF-e, NFC-e e eventos) produzidos
/// pelos parsers de leiaute (ex.: <c>TecnoFisc.Sped.NFeNFCe</c>). Permite que o fluxo genérico
/// (<c>ParserNFe.ReadAsync</c>/<c>ReadDirectoryAsync</c>) e o <c>Correlator</c> tratem qualquer
/// documento pela sua chave de acesso sem conhecer o tipo concreto (o consumidor faz pattern
/// matching para os tipos específicos). Transversal a leiautes XML fiscais (NF-e/NFC-e hoje,
/// CT-e no futuro) — por isso reside no Core, ao lado de <see cref="IdentificadorXmlFiscal"/>.
/// </summary>
public interface IDocumentoFiscalXml
{
    /// <summary>
    /// Chave de acesso de 44 dígitos do documento (em eventos, a chave referenciada em
    /// <c>chNFe</c>). É a chave de correlação entre uma nota e seus eventos.
    /// </summary>
    ChaveAcesso ChaveAcesso { get; }
}
```

- [ ] **Step 4: Rodar o teste e confirmar que passa**

Run: `dotnet test TecnoFisc.Sped.slnx --filter "FullyQualifiedName~IDocumentoFiscalXml"`
Expected: PASS (1 teste). Build com 0 warnings.

- [ ] **Step 5: Commit**

```bash
git add src/TecnoFisc.Sped.Core/Xml/IDocumentoFiscalXml.cs tests/TecnoFisc.Sped.Core.Tests/Xml/IDocumentoFiscalXmlTests.cs
git commit -m "feat(core): IDocumentoFiscalXml — contrato comum dos documentos fiscais XML (slice 14.2)"
```

---

## Task 3: `ParserNFeOptions`

Opções de leitura do parser (§3 do STAGE_14): `Strict`, `ValidateChecksums`, `Parallelism`, `FailFast`, com instância `Default`.

**Files:**
- Create: `src/TecnoFisc.Sped.NFeNFCe/ParserNFeOptions.cs`
- Test: `tests/TecnoFisc.Sped.NFeNFCe.Tests/ParserNFeOptionsTests.cs`

- [ ] **Step 1: Escrever os testes que falham**

`tests/TecnoFisc.Sped.NFeNFCe.Tests/ParserNFeOptionsTests.cs`:

```csharp
namespace TecnoFisc.Sped.NFeNFCe.Tests;

public class ParserNFeOptionsTests
{
    [Fact]
    public void Default_Has_Expected_Values()
    {
        var opcoes = ParserNFeOptions.Default;

        opcoes.Strict.Should().BeFalse();
        opcoes.ValidateChecksums.Should().BeTrue();
        opcoes.Parallelism.Should().Be(Environment.ProcessorCount);
        opcoes.FailFast.Should().BeTrue();
    }

    [Fact]
    public void Init_Overrides_Are_Applied()
    {
        var opcoes = new ParserNFeOptions
        {
            Strict = true,
            ValidateChecksums = false,
            Parallelism = 4,
            FailFast = false,
        };

        opcoes.Strict.Should().BeTrue();
        opcoes.ValidateChecksums.Should().BeFalse();
        opcoes.Parallelism.Should().Be(4);
        opcoes.FailFast.Should().BeFalse();
    }

    [Theory]
    [InlineData(0)]
    [InlineData(-1)]
    public void Parallelism_Below_One_Throws(int invalido)
    {
        Action act = () => _ = new ParserNFeOptions { Parallelism = invalido };

        act.Should().Throw<ArgumentOutOfRangeException>();
    }
}
```

- [ ] **Step 2: Rodar e confirmar a falha**

Run: `dotnet test TecnoFisc.Sped.slnx --filter "FullyQualifiedName~ParserNFeOptions"`
Expected: FALHA de compilação — `ParserNFeOptions` não existe.

- [ ] **Step 3: Implementar `ParserNFeOptions`**

`src/TecnoFisc.Sped.NFeNFCe/ParserNFeOptions.cs`:

```csharp
namespace TecnoFisc.Sped.NFeNFCe;

/// <summary>
/// Opções de leitura do <see cref="ParserNFe"/>. Imutável (init-only) e reaproveitável entre
/// chamadas — o parser é stateless. Use <see cref="Default"/> para o comportamento padrão.
/// </summary>
public sealed class ParserNFeOptions
{
    private readonly int _parallelism = Environment.ProcessorCount;

    /// <summary>Instância padrão (todos os defaults).</summary>
    public static ParserNFeOptions Default { get; } = new();

    /// <summary>
    /// Quando <c>false</c> (padrão), elementos desconhecidos são ignorados em vez de lançar —
    /// protege contra Notas Técnicas futuras que acrescentem campos. Quando <c>true</c>, um
    /// elemento não mapeado interrompe a leitura.
    /// </summary>
    public bool Strict { get; init; }

    /// <summary>
    /// Quando <c>true</c> (padrão), valida o dígito verificador da chave de acesso e demais
    /// value objects (GTIN, etc.) na construção do modelo.
    /// </summary>
    public bool ValidateChecksums { get; init; } = true;

    /// <summary>
    /// Grau de paralelismo de <c>ReadDirectoryAsync</c> (caminho "milhões de XMLs"). Padrão =
    /// <see cref="Environment.ProcessorCount"/>. Deve ser maior ou igual a 1.
    /// </summary>
    public int Parallelism
    {
        get => _parallelism;
        init
        {
            ArgumentOutOfRangeException.ThrowIfLessThan(value, 1);
            _parallelism = value;
        }
    }

    /// <summary>
    /// Quando <c>true</c> (padrão), a primeira falha de parse interrompe a leitura. Quando
    /// <c>false</c>, em <c>ReadDirectoryAsync</c> o arquivo com erro é logado e pulado.
    /// </summary>
    public bool FailFast { get; init; } = true;
}
```

- [ ] **Step 4: Rodar e confirmar que passa**

Run: `dotnet test TecnoFisc.Sped.slnx --filter "FullyQualifiedName~ParserNFeOptions"`
Expected: PASS (4 testes). Build 0 warnings.

- [ ] **Step 5: Commit**

```bash
git add src/TecnoFisc.Sped.NFeNFCe/ParserNFeOptions.cs tests/TecnoFisc.Sped.NFeNFCe.Tests/ParserNFeOptionsTests.cs
git commit -m "feat(nfe): ParserNFeOptions (slice 14.2)"
```

---

## Task 4: Esqueleto de `ParserNFe`

Casca do parser: construtores (com null-check), `Options` exposto, e `ReadAsync` que valida o stream e lança `NotImplementedException` (corpo real entra na slice 14.3). Thread-safe e sem estado mutável.

**Files:**
- Create: `src/TecnoFisc.Sped.NFeNFCe/ParserNFe.cs`
- Test: `tests/TecnoFisc.Sped.NFeNFCe.Tests/ParserNFeTests.cs`

- [ ] **Step 1: Escrever os testes que falham**

`tests/TecnoFisc.Sped.NFeNFCe.Tests/ParserNFeTests.cs` (sem `using System.IO;` — `MemoryStream`/`Environment` vêm do `ImplicitUsings`; `ReadAsync` recebe o `TestContext.Current.CancellationToken` por causa do xUnit1051):

```csharp
namespace TecnoFisc.Sped.NFeNFCe.Tests;

public class ParserNFeTests
{
    [Fact]
    public void Default_Ctor_Uses_Default_Options()
    {
        var parser = new ParserNFe();

        parser.Options.Should().BeSameAs(ParserNFeOptions.Default);
    }

    [Fact]
    public void Ctor_Stores_Provided_Options()
    {
        var opcoes = new ParserNFeOptions { Strict = true };

        var parser = new ParserNFe(opcoes);

        parser.Options.Should().BeSameAs(opcoes);
    }

    [Fact]
    public void Ctor_Null_Options_Throws()
    {
        Action act = () => _ = new ParserNFe(null!);

        act.Should().Throw<ArgumentNullException>();
    }

    [Fact]
    public async Task ReadAsync_Null_Stream_Throws()
    {
        var parser = new ParserNFe();

        Func<Task> act = () => parser.ReadAsync(null!, TestContext.Current.CancellationToken);

        await act.Should().ThrowAsync<ArgumentNullException>();
    }

    // Esqueleto da slice 14.2: o corpo de desserialização chega na 14.3 e este teste será
    // substituído por testes E2E de leitura real.
    [Fact]
    public async Task ReadAsync_Valid_Stream_NotImplemented_Until_14_3()
    {
        var parser = new ParserNFe();
        using var stream = new MemoryStream([1, 2, 3]);

        Func<Task> act = () => parser.ReadAsync(stream, TestContext.Current.CancellationToken);

        await act.Should().ThrowAsync<NotImplementedException>();
    }
}
```

- [ ] **Step 2: Rodar e confirmar a falha**

Run: `dotnet test TecnoFisc.Sped.slnx --filter "FullyQualifiedName~ParserNFeTests"`
Expected: FALHA de compilação — `ParserNFe` não existe.

- [ ] **Step 3: Implementar o esqueleto de `ParserNFe`**

`src/TecnoFisc.Sped.NFeNFCe/ParserNFe.cs`:

```csharp
using TecnoFisc.Sped.Core.Xml;

namespace TecnoFisc.Sped.NFeNFCe;

/// <summary>
/// Leitor dos documentos fiscais XML do leiaute 4.00 (NF-e modelo 55, NFC-e modelo 65 e
/// eventos), read-only. Thread-safe e sem estado mutável — uma instância pode ser compartilhada
/// entre threads. A desserialização é <b>order-independent</b> (tolera tanto o XML canônico
/// quanto a reordenação do envelope SERPRO); ver <c>sped/STAGE_14_NFE_NFCE.md</c> §3.
/// </summary>
/// <remarks>
/// Esqueleto da slice 14.2: o corpo de leitura entra a partir da slice 14.3.
/// </remarks>
public sealed class ParserNFe
{
    /// <summary>Cria o parser com as opções padrão (<see cref="ParserNFeOptions.Default"/>).</summary>
    public ParserNFe() : this(ParserNFeOptions.Default)
    {
    }

    /// <summary>Cria o parser com opções customizadas.</summary>
    public ParserNFe(ParserNFeOptions opcoes)
    {
        ArgumentNullException.ThrowIfNull(opcoes);
        Options = opcoes;
    }

    /// <summary>Opções com que este parser foi construído.</summary>
    public ParserNFeOptions Options { get; }

    /// <summary>
    /// Lê um documento fiscal de <paramref name="entrada"/> e devolve o modelo tipado pelo
    /// contrato comum <see cref="IDocumentoFiscalXml"/> — o consumidor faz pattern matching para
    /// os tipos concretos (NF-e/NFC-e/evento).
    /// </summary>
    /// <param name="entrada">Stream do XML (canônico ou envelope SERPRO).</param>
    /// <param name="cancelamento">Token de cancelamento.</param>
    public Task<IDocumentoFiscalXml> ReadAsync(Stream entrada, CancellationToken cancelamento = default)
    {
        ArgumentNullException.ThrowIfNull(entrada);
        throw new NotImplementedException(
            "Desserialização entra a partir da slice 14.3 (ver sped/STAGE_14_NFE_NFCE.md §8).");
    }
}
```

> Se — e somente se — o build acusar CA1822 como **erro** em `ReadAsync` (improvável; CA1822 é info-level), adicione `_ = Options;` como primeira linha após o null-check. Verifique pelo build, não preventivamente.

- [ ] **Step 4: Rodar e confirmar que passa**

Run: `dotnet test TecnoFisc.Sped.slnx --filter "FullyQualifiedName~ParserNFeTests"`
Expected: PASS (5 testes). Build 0 warnings.

- [ ] **Step 5: Commit**

```bash
git add src/TecnoFisc.Sped.NFeNFCe/ParserNFe.cs tests/TecnoFisc.Sped.NFeNFCe.Tests/ParserNFeTests.cs
git commit -m "feat(nfe): esqueleto de ParserNFe (slice 14.2)"
```

---

## Task 5: Deltas no `ARCHITECTURE.md` (fusão Stage 14+15)

Aplica os 6 deltas do §0 do STAGE_14: funde Stage 14 (NFe) + Stage 15 (NFCe) em um Stage 14 `NFeNFCe`, **sem renumerar** Stage 16/17. Edições de doc — sem testes; valida-se por leitura.

**Files:**
- Modify: `ARCHITECTURE.md`

- [ ] **Step 1: Delta 1 — tabela read-only (§2.5, linha ~101)**

Trocar:

```
| `TecnoFisc.Sped.NFe` / `NFCe` / `CTe` | ✅ | ⏸️ | Caso de uso confirmado é ingestão dos XMLs já emitidos (validação de assinatura, leitura tipada). Geração/emissão para SEFAZ depende de confirmação externa não controlada só pelo usuário — entra em stage dedicada quando confirmada. |
```

Por:

```
| `TecnoFisc.Sped.NFeNFCe` / `CTe` | ✅ | ⏸️ | Caso de uso confirmado é ingestão dos XMLs já emitidos (validação de assinatura, leitura tipada). Geração/emissão para SEFAZ depende de confirmação externa não controlada só pelo usuário — entra em stage dedicada quando confirmada. |
```

- [ ] **Step 2: Delta 2 — tabela de escopo (§3, linhas ~125-126)**

Trocar as duas linhas:

```
| NF-e | `TecnoFisc.Sped.NFe` | XML (UTF-8) |
| NFC-e | `TecnoFisc.Sped.NFCe` | XML (UTF-8) |
```

Por uma só:

```
| NF-e / NFC-e | `TecnoFisc.Sped.NFeNFCe` | XML (UTF-8) |
```

- [ ] **Step 3: Delta 3 — mapa de pastas (§6, linhas ~244-245)**

Trocar:

```
│   ├── TecnoFisc.Sped.NFe/                           # NF-e XML
│   ├── TecnoFisc.Sped.NFCe/                          # NFC-e XML
```

Por:

```
│   ├── TecnoFisc.Sped.NFeNFCe/                       # NF-e / NFC-e XML (leiaute 4.00, read-only)
```

- [ ] **Step 4: Delta 4 — regras de dependência (§6.1, linhas ~270-271)**

Trocar:

```
TecnoFisc.Sped.NFe                   ← Core, Core.SourceGenerators (analyzer)
TecnoFisc.Sped.NFCe                  ← Core, Core.SourceGenerators (analyzer)
```

Por (uma linha, **sem** source generator — não há `RegistroSped` na v1):

```
TecnoFisc.Sped.NFeNFCe               ← Core
```

- [ ] **Step 5: Delta 5 — metapacote (§13, linha ~658)**

Trocar:

```
- Sem código próprio — apenas `<PackageReference>` para cada um dos pacotes de leiaute (`EfdContribuicoes`, `EfdIcmsIpi`, `Ecd`, `NFe`, `NFCe`, `CTe`, `Ecf`).
```

Por:

```
- Sem código próprio — apenas `<PackageReference>` para cada um dos pacotes de leiaute (`EfdContribuicoes`, `EfdIcmsIpi`, `Ecd`, `NFeNFCe`, `CTe`, `Ecf`).
```

- [ ] **Step 6: Delta 6 — fundir os cabeçalhos Stage 14 + Stage 15 (linhas ~664-674)**

Trocar o bloco:

```
### Stage 14 — TecnoFisc.Sped.NFe (XML, **read-only**)

- XML parser baseado em `System.Xml.Linq` (ou `XmlReader` para arquivos grandes em modo streaming).
- Classes de modelo fortemente tipadas mapeando o schema NF-e (procNFe, infNFe, det, total, transp, cobr, infAdic, etc.).
- Validação de assinatura digital (apenas validação — não assinatura).
- Encoding canônico do XML = UTF-8 (diferente dos `.txt` SPED que são Latin1).
- **Modo read-only (§2.5).** Sem `GeradorNFe`, sem emissão para SEFAZ, sem round-trip parse→generate→parse. Caso de uso confirmado é ingestão dos XMLs já emitidos. Geração/emissão entra em stage dedicada se confirmada por demanda externa.

### Stage 15 — TecnoFisc.Sped.NFCe (XML, **read-only**)

Estrutura idêntica a Stage 14, schema NFC-e (Nota Fiscal de Consumidor Eletrônica, modelo 65). Muito código pode ser compartilhado com NF-e via base classes em `TecnoFisc.Sped.Core/Xml/`, mas os tipos públicos do leiaute ficam no pacote NFCe. Modo read-only (§2.5) — sem `GeradorNFCe`.
```

Por:

```
### Stage 14 — TecnoFisc.Sped.NFeNFCe (XML, **read-only**)

**Funde os antigos Stage 14 (NFe) + Stage 15 (NFCe) em um único pacote** `TecnoFisc.Sped.NFeNFCe`, leiaute 4.00. NF-e (modelo 55) e NFC-e (modelo 65) usam o **mesmo XSD** e evoluem juntas (mesma Nota Técnica) — não se encaixam na premissa de cadências independentes que justifica a regra de independência de formato (§4.2). Logo: um pacote, dois tipos de modelo (`NFe`, `NFCe`). Spec operacional completa em `sped/STAGE_14_NFE_NFCE.md`.

- Parser `XmlReader` forward-only, **order-independent** (loop `switch (reader.LocalName)`) — lê o XML canônico e o envelope SERPRO na mesma velocidade. Sem DOM por arquivo.
- Classes de modelo fortemente tipadas mapeando o leiaute (ide, emit, dest, det/prod, imposto polimórfico, total, transp, cobr, pag, infAdic, protNFe), nativas da NF-e (não moldadas em registro SPED).
- NFC-e ≈ NF-e + `infNFeSupl` (QR Code) + `dest` opcional. Tipos distintos, sem polimorfismo entre os dois.
- Eventos (`procEventoNFe`/`eventoNFe`): `EventoCancelamento` tipado + `EventoGenerico` fallback; correlação nota × eventos por `ChaveAcesso` (read-only/stateless).
- Validação de assinatura digital fora da v1 (apenas leitura). Encoding canônico do XML = UTF-8.
- **Modo read-only (§2.5).** Sem `GeradorNFe`/`GeradorNFCe`, sem emissão para SEFAZ, sem round-trip. **Sem source generator na v1** (não há `RegistroSped`). Caso de uso confirmado é ingestão dos XMLs já emitidos.

### Stage 15 — absorvido pelo Stage 14

O antigo Stage 15 (NFC-e como pacote separado) foi **absorvido pelo Stage 14** (pacote único `TecnoFisc.Sped.NFeNFCe`). Número mantido para não renumerar os stages a jusante (Stage 16 CT-e, Stage 17 ECF inalterados).
```

- [ ] **Step 7: Revisar coerência**

Run: `git diff ARCHITECTURE.md`
Verificar: nenhuma referência sobrante a `TecnoFisc.Sped.NFe ` ou `TecnoFisc.Sped.NFCe ` como pacotes separados (exceto a nota "absorvido"); Stage 16/17 intactos.

Grep de sanidade:

Run: `grep -nE "TecnoFisc\.Sped\.NFe |TecnoFisc\.Sped\.NFCe |Sped\.NFe/|Sped\.NFCe/" ARCHITECTURE.md`
Expected: nenhuma saída (todas as ocorrências foram fundidas em `NFeNFCe`).

- [ ] **Step 8: Commit**

```bash
git add ARCHITECTURE.md
git commit -m "docs: fundir Stage 14+15 em TecnoFisc.Sped.NFeNFCe (deltas §0 slice 14.2)"
```

---

## Task 6: Finalização — build/test completos e tracking

**Files:**
- Modify: `sped/STAGE_14_NFE_NFCE.md` (marcar 14.2 como feito)

- [ ] **Step 1: Build + test da solução inteira**

Run: `dotnet build TecnoFisc.Sped.slnx`
Expected: 0 errors, 0 warnings.

Run: `dotnet test TecnoFisc.Sped.slnx`
Expected: todos passam (Core, Ecd, EfdContribuicoes, EfdIcmsIpi + os novos testes de NFeNFCe), 0 falhas.

- [ ] **Step 2: Marcar a slice 14.2 como concluída em `sped/STAGE_14_NFE_NFCE.md`**

Na tabela §8, trocar a linha da 14.2 de `| [ ] | 14.2 |` para `| [x] | 14.2 |`.

- [ ] **Step 3: Commit**

```bash
git add sped/STAGE_14_NFE_NFCE.md
git commit -m "docs: marcar slice 14.2 (estrutura do pacote) como concluída"
```

---

## Notas de execução

- **Não** adicionar `ReadNFeAsync`/`ReadNFCeAsync`/`ReadEventoAsync`/`ReadDirectoryAsync` nesta slice: dependem dos tipos `NFe`/`NFCe`/`Evento` (slices 14.3/14.7) e do paralelismo de diretório (14.8). A casca mínima é ctor + `Options` + `ReadAsync` genérico.
- Namespace raiz `TecnoFisc.Sped.NFeNFCe` para `ParserNFe`/`ParserNFeOptions` (API plana). `IDocumentoFiscalXml` em `TecnoFisc.Sped.Core.Xml`.
- Convenção de nomes (CLAUDE.md §1.3): substantivos do domínio em PT, verbos/predicados em EN. `ReadAsync`, `Create` (EN); `Options` é nome de capability técnica (EN, coerente com o tipo `ParserNFeOptions`).
